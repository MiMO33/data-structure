#include"bubble_sort.h"

void sort(const void* base, 
          const size_t length, 
          const size_t element_size, 
          int* (compare)(void* a, void* b))
{
    assert(length % element_size == 0);
}

static void swap(const void* start,const size_t element_size)
{

}